describe("category module", () => {
  describe("the name of the category to display", () => {
    it("should display the name of the category", () => {
      let category = require('../../../components/categories.js')
    })
  })
})
